version: 2

sources:
  - name: stripe
    database: dbt_learn_asvenkeson
    schema: stripe
    tables:
      - name: payment
        loaded_at_field: _batched_at
        freshness: 
            warn_after: {count: 12 , period: hour} #if max loaded at field is greater than 12 hours old gives a warning
            error_after: {count: 24, period: hour} # if data is older than 24 hours, don't run anything else